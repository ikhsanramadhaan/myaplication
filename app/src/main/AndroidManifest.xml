<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ikhsanramadhan.myaplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".kelas01Activity" android:label="@string/kelas01"/>
        <activity android:name=".kelas02Activity" android:label="@string/kelas02" />
        <activity android:name=".kelas03Activity" android:label="@string/kelas03"/>
        <activity android:name=".kelas04Activity" android:label="@string/kelas04">

        </activity>
    </application>

</manifest>